import PropTypes from 'prop-types';


const Readingtime = ({handleReadingTime}) => {

    console.log(handleReadingTime)
    // console.log(reading_time)
    return (
        <div>
           
        </div>
    );
};

Readingtime.propTypes = {
    handleReadingTime : PropTypes.number.isRequired
}

export default Readingtime;